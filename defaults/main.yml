---
# defaults file for role
# Paths to volumes to create (only when using the "bind" type)
compose_deploy_volumes_dir: []

# Compose projects to copy and deploy on the remote host
compose_deploy_projects: []
